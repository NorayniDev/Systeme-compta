spring:
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true

logging:
  level:
    com.facturationpme: DEBUG

app:
  jwt:
    secret: test-secret-key-not-for-production-use-only-in-automated-tests-1234567890
